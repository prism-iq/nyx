// fizzbuzz.flow - Classic FizzBuzz

to start:
    for each i in 1 to 30:
        if i % 15 is 0:
            say "FizzBuzz"
        otherwise if i % 3 is 0:
            say "Fizz"
        otherwise if i % 5 is 0:
            say "Buzz"
        otherwise:
            say i
